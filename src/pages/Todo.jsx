import "./styles/main.scss";

import Form from "react-bootstrap/Form"
// import { useState } from "react";

const Todo = () => {

  return (
    <>
      <h1>Todo</h1>
   
    </>
  );
};

export default Todo;

{
  /* const [isClicked, setIsClicked] = useState(false);
//   const [isClicked, setIsClicked] = useState([
//     { id: 1, text: "Todo 1", isCompleted: false },
//     { id: 2, text: "Todo 2", isCompleted: false },
//     { id: 3, text: "Todo 3", isCompleted: false },
//   ]);

  function handleClick() {
    //setIsClicked(true) -> verändert nur einmal den Zustand
    setIsClicked((prev) => !prev); //eine Funktion, die immer gegenteilig ist von aktuellen Zustand
  } 

    return

  <div>
<input type="text" placeholder="Add todo" />
<button>Add</button>
</div>

<div>
<ul>
  <li>
    <input type="checkbox" onClick={handleClick} />
    <span
      style={{ textDecoration: isClicked ? "line-through" : "none" }}
    >
      Todo 1
    </span>
    <button>Delete</button>
  </li>
  <li>
    <input type="checkbox" onClick={handleClick} />
    <span
      style={{ textDecoration: isClicked ? "line-through" : "none" }}
    >
      Todo 2
    </span>
    <button>Delete</button>
  </li>
  <li>
    <input type="checkbox" onClick={handleClick} />
    <span
      style={{ textDecoration: isClicked ? "line-through" : "none" }}
    >
      Todo 3
    </span>
    <button>Delete</button>
  </li>
</ul>
</div> */
}
